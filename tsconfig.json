{
  "compilerOptions": {
    "target": "es2023",
    "module": "esnext",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    /* `node` is here for scripts/, which runs under plain Node. App code should
       still not reach for Node APIs — nothing in src/ needs them. */
    "types": ["vite/client", "vite-plugin-pwa/react", "node"],
    "jsx": "react-jsx",
    "allowArbitraryExtensions": true,
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,

    /* NOTE: deliberately no `baseUrl`. Vite+'s type-aware lint runs through
       tsgolint, which does not support it and silently disables `typeAware` /
       `typeCheck` when it is present. TS >= 5.4 resolves `paths` without it. */
    "paths": { "@/*": ["./src/*"] },

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src", "scripts"]
}
